package Baitapbuoi6.BaitapString;

import java.util.Scanner;

public class baitap {
    public static void main(String[] args) {
        // Bài 1 Nhập vào một chuỗi bất kỳ, hiển thị chuỗi đảo ngược của chuỗi vừa nhập

//            Scanner scanner = new Scanner(System.in);
//            System.out.println("Nhập chuỗi kí tự: ");
//            String truoc = scanner.nextLine();
//            String sau = new StringBuffer(truoc).reverse().toString();
//            System.out.println("Chuỗi khi chưa đảo ngược: " + truoc);
//            System.out.println("Chuỗi khi được đảo ngược: " + sau);


        // Bài 2:  Nhập vào chuỗi bất kỳ kiểm tra xem chuỗi có chứa chữ số không

//        Scanner scanner = new Scanner(System.in);
//        System.out.println("Nhập chuỗi kí tự: ");
//        String input = scanner.nextLine();
//        boolean sokitu = true;
//        for (int i = 0; i < input.length(); i++) {
//            if (input.charAt(i) >= 48 && input.charAt(i) <= 57 ) {
//                sokitu = false;
//                break;
//            }
//        }
//        if (sokitu) {
//            System.out.println("không chưa kí tự số:");
//        } else {
//            System.out.println("chứa kí tự số:");
//        }



        // Bài 3) Kiểm tra 2 chuỗi có là đảo ngược của nhau hay không.
        //     Nếu có xuất ra “OK” ngược lại “KO”.
        //     Ví dụ “word” và “drow” là 2 chuỗi đảo ngược nha

//        Scanner scanner = new Scanner(System.in);
//        System.out.println("Nhập chuỗi kí tự 1: ");
//        String t1 = scanner.nextLine();
//        System.out.println("Nhập chuỗi kí tự 1: ");
//        String t2 = scanner.nextLine();
//        StringBuffer stringBuffer = new StringBuffer(t1);
//        if (stringBuffer.reverse().toString().equals(t2)) {
//            System.out.println("Hai chuỗi là đảo ngược: ");
//        } else {
//            System.out.println("Hai chuỗi không là đảo ngược: ");
//        }


        // Bài 4: Tìm và in ra các ký tự xuất hiện nhiều hơn một lần trong String
        // cho trước không phân biệt chữ hoa hay chữ thường.
        // Nếu các ký tự trong chuỗi đều là duy nhất thì xuất ra “NO”.
        // Ví dụ chuỗi “Java” thì có ký tự ‘a’ hoặc String “JaVA” cũng có kết quả tương tự.

    }
}
